{
  "name": "hackerhouse-streams",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ hackerhouse-streams",
    "create-local": "graph create --node http://localhost:8020/ hackerhouse-streams",
    "remove-local": "graph remove --node http://localhost:8020/ hackerhouse-streams",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hackerhouse-streams",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.48.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
